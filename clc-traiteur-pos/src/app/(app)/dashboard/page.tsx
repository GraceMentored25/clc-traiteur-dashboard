import DashboardClient from "@/components/dashboard/DashboardClient";

export const metadata = { title: "Création de devis — C.LC. Traiteur" };

export default function DashboardPage() {
  return <DashboardClient />;
}
